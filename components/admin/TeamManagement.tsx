"use client";
import Link from "next/link";import {useEffect,useState} from "react";
type Staff={id:string;email:string;display_name:string|null;role:string;active:boolean};
const roles=["manager","sales","finance","operations","content_editor"];
/* eslint-disable react-hooks/set-state-in-effect -- the owner-only staff list is loaded from an external API after mount */
export default function TeamManagement(){const[staff,setStaff]=useState<Staff[]>([]);const[form,setForm]=useState({name:"",email:"",role:"sales",mode:"invite",password:""});const[message,setMessage]=useState("");const load=async()=>{const response=await fetch("/api/admin/staff-accounts");const data=await response.json();if(response.ok)setStaff(data.staff||[]);};useEffect(()=>{void load()},[]);async function submit(e:React.FormEvent){e.preventDefault();const response=await fetch("/api/admin/staff-accounts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await response.json();setMessage(response.ok?(form.mode==="invite"?"Invitation sent.":"Account created."):(data.error||"Could not create account."));if(response.ok){setForm({...form,name:"",email:"",password:""});await load();}}async function update(item:Staff,patch:Partial<Staff>){await fetch("/api/admin/staff-accounts",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:item.id,role:patch.role||item.role,active:patch.active??item.active})});await load();}return <><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-bold uppercase tracking-widest text-cyan-700">Administration</p><h1 className="mt-2 text-3xl font-black">Team accounts</h1></div><div className="flex gap-2"><Link href="/admin/account" className="rounded-xl border px-4 py-2 text-sm font-bold">Change my password</Link><Link href="/admin" className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white">Back to admin</Link></div></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_.7fr]"><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Current users</h2><div className="mt-4 divide-y">{staff.map(item=><div key={item.id} className="flex flex-wrap items-center justify-between gap-3 py-4"><div><p className="font-bold">{item.display_name||item.email}</p><p className="text-sm text-slate-500">{item.email}</p></div><div className="flex gap-2"><select disabled={item.role==="owner"} value={item.role} onChange={e=>void update(item,{role:e.target.value})} className="rounded-lg border p-2 text-sm"><option value="owner">owner</option>{roles.map(role=><option key={role}>{role}</option>)}</select><button disabled={item.role==="owner"} onClick={()=>void update(item,{active:!item.active})} className={`rounded-lg px-3 py-2 text-sm font-bold ${item.active?"bg-rose-50 text-rose-700":"bg-emerald-50 text-emerald-700"}`}>{item.active?"Deactivate":"Reactivate"}</button></div></div>)}</div></section><form onSubmit={submit} className="h-fit rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Add user</h2><select value={form.mode} onChange={e=>setForm({...form,mode:e.target.value})} className="mt-4 w-full rounded-xl border p-3"><option value="invite">Email invitation</option><option value="create">Create with password</option></select><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Full name" className="mt-3 w-full rounded-xl border p-3"/><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email" className="mt-3 w-full rounded-xl border p-3"/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="mt-3 w-full rounded-xl border p-3">{roles.map(role=><option key={role}>{role}</option>)}</select>{form.mode==="create"?<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Temporary strong password" className="mt-3 w-full rounded-xl border p-3"/>:null}<button className="mt-4 w-full rounded-xl bg-cyan-700 p-3 font-bold text-white">{form.mode==="invite"?"Send invitation":"Create account"}</button>{message?<p role="status" className="mt-3 text-sm font-semibold">{message}</p>:null}</form></div></>}
