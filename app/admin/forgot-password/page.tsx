"use client";
import {useState} from "react";import Link from "next/link";
export default function ForgotPasswordPage(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><form onSubmit={async e=>{e.preventDefault();await fetch("/api/admin/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setSent(true);}} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-sm"><h1 className="text-2xl font-black">Reset admin password</h1><p className="mt-2 text-sm text-slate-500">We will send a secure reset link if this account exists.</p>{sent?<p role="status" className="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-800">Check your email for the reset link.</p>:<><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="mt-5 w-full rounded-xl border p-3"/><button className="mt-3 w-full rounded-xl bg-slate-900 p-3 font-bold text-white">Send reset link</button></>}<Link href="/admin/login" className="mt-5 block text-center text-sm font-bold text-cyan-700">Back to sign in</Link></form></main>}
