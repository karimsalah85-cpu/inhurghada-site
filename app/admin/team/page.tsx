import {redirect} from "next/navigation";import TeamManagement from "@/components/admin/TeamManagement";import PermissionsMatrix from "@/components/admin/PermissionsMatrix";import {isAdminOwner} from "@/lib/admin-auth";import {createClient} from "@/utils/supabase/server";
export default async function TeamPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/admin/login");if(!isAdminOwner(user))redirect("/admin");return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-7xl"><TeamManagement/><PermissionsMatrix/></div></main>}
