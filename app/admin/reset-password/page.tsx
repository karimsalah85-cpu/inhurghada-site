"use client";
import {useEffect,useMemo,useState} from "react";import {createClient} from "@/utils/supabase/client";import {passwordPolicyError} from "@/lib/password-policy";
export default function ResetPasswordPage(){const[password,setPassword]=useState("");const[message,setMessage]=useState("");const supabase=useMemo(()=>createClient(),[]);useEffect(()=>{const code=new URLSearchParams(window.location.search).get("code");if(code)void supabase.auth.exchangeCodeForSession(code);},[supabase]);return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><form onSubmit={async e=>{e.preventDefault();const invalid=passwordPolicyError(password);if(invalid)return setMessage(invalid);const{error}=await supabase.auth.updateUser({password});setMessage(error?error.message:"Password updated. You can now sign in.");}} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-sm"><h1 className="text-2xl font-black">Choose a new password</h1><p className="mt-2 text-sm text-slate-500">At least 12 characters with upper/lowercase, a number, and a symbol.</p><input required type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-5 w-full rounded-xl border p-3"/><button className="mt-3 w-full rounded-xl bg-slate-900 p-3 font-bold text-white">Update password</button>{message?<p role="status" className="mt-4 text-sm font-semibold">{message}</p>:null}</form></main>}
